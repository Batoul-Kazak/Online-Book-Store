import RateUs from "@/components/ui/RateUs/RateUs";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Rate Us Page"
}

export default function RateUsPage() {
  return <RateUs />
}